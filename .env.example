# Optional live-source credentials for future/adapted studies.
# Cached reproduction does not require any secrets.

# X_BEARER_TOKEN=replace-with-your-token
# X_API_KEY=replace-with-your-key
# X_API_SECRET=replace-with-your-secret

# If using xurl, configure xurl locally outside this repository.
# Do not commit .xurl, OAuth token files, or auth directories.
